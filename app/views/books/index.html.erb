<h1>Books!</h1>
<%= link_to "New Book", new_book_path %>

<div>
  <ul>
    <% @books.each do |book| %>
    <li><%= link_to book.title, book_path(book) %> (<%= book.pages %> pages)</li>
    <% end %>
  </ul>
</div>
<%= link_to "All Authors", authors_path %>
